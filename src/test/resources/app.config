#browser
#browser = chrome

browser = firefox:64

#browser.headlessMode=false

#framework.pageload.timeout=5
#framework.findelement.timeout=5

framework.web.finder.max_tentativas = 10
framework.web.finder.tempo_entre_tentativas = 10000

browser.web.fechar_ao_final = false
